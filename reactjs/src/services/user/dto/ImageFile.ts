export interface ImageFile {
    ImageData: string | null; 
    ImageName: string | null; 
    placeId: string | null;  
    isRating: boolean;  
    ratingId: number;  
    moreRatingId: number;  

  }